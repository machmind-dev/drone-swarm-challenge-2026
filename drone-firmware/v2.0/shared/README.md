# Shared Protocol

UART protocol definitions shared between `drone-vision-esp32p4` (Navigation) and `drone-comms-esp32s3` (Communication).

## Contents

- `uart_protocol.h` — message structs for P4→S3 pose and ToF data
